import 'package:flutter/material.dart';

class UvIndexScreen extends StatelessWidget {
  const UvIndexScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('UvIndexScreen'),
      ),
      body: Center(
        child: Text('UvIndexScreen'),
      ),
    );
  }
}
